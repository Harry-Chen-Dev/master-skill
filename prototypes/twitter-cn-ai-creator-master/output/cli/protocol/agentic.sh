#!/usr/bin/env bash
# agentic.sh — Agentic Protocol CLI for 推特中文圈 AI 自媒体博主.
# Auto-generated by cli_writer.py. Source: synthesis.md Section 9.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=../lib/common.sh
source "${SCRIPT_DIR}/../lib/common.sh"

INDUSTRY="推特中文圈 AI 自媒体博主"
DIM_COUNT=6

usage() {
  cat <<EOF
agentic.sh — Agentic Protocol CLI for ${INDUSTRY}

拿到 ${INDUSTRY} 行业的新问题时，按 ${DIM_COUNT} 个研究维度做功课再答。
不直接给答案，而是引导你用资深从业者的研究维度收集信息。

USAGE:
  ./agentic.sh                # interactive walkthrough
  ./agentic.sh --dry-run      # walk through but skip file write
  ./agentic.sh --json         # emit JSON to stdout
  ./agentic.sh --explain      # show backing mental models + sources
  ./agentic.sh --help         # this message
EOF
}

explain() {
  cat <<'EOT'
这个 Agentic Protocol 把这一行人面对新问题的研究维度结构化。

研究维度 (6 个):
  9.1 选题与信息源
  9.2 编译与重写
  9.3 推文/thread 结构
  9.4 配图与工具
  9.5 发布与算法适配
  9.6 互动与涨粉复盘

来源: synthesis.md Section 9 + Section 1.
EOT
}

DRY_RUN=0; JSON=0
for arg in "$@"; do
  case "$arg" in
    --help|-h) usage; exit 0 ;;
    --explain) explain; exit 0 ;;
    --dry-run) DRY_RUN=1 ;;
    --json) JSON=1 ;;
  esac
done

declare -a DIM_TITLES=(
  "选题与信息源"
  "编译与重写"
  "推文/thread 结构"
  "配图与工具"
  "发布与算法适配"
  "互动与涨粉复盘"
)
declare -a DIM_QUESTIONS=(
  "英文一手(arXiv/官博/HN/Reddit)有什么新的、信息差大不大、有没有本地洞察空间。"
  "理解原意、按 voice 中文重写、加独有洞察/实测、标来源。"
  "单条 vs thread、首条 hook、信息密度、排版断行、CTA。"
  "配图类型(代码 carbon/截图/对比图)、发文/排程工具、避撞脸。"
  "发布时机、外链位置(放自回复)、Premium/长推、避降权与举报。"
  "作者回复(最高权重)、引发讨论、impressions/转评赞、build-in-public 飞轮。"
)
declare -a DIM_SOURCES=(
  "Folo/RSS 盯英文一手源 + X 热点。"
  "Track 03 编译三阶段 + Track 01 宝玉/歸藏方法。"
  "Track 04 写作 canon(hook 公式) + Track 03 thread SOP。"
  "Track 02 工具决策树。"
  "xai-org/x-algorithm + X 官方(Track 04/05)。"
  "X Analytics + 互动数据。"
)
declare -a DIM_OUTPUT_FORMATS=(
  "选定选题 + 信息差判断 + 中文化洞察角度。"
  "中文重写稿 + 加的洞察 + 来源标注（绝非机翻）。"
  "hook（前 280 字自足）+ 结构（编号逻辑链）+ 结尾 CTA。"
  "工具组合 + 配图方案 + AI 介入度(重写助手非代笔)。"
  "发布计划 + 外链放自回复 + 算法适配清单(标 disputed 值)。"
  "互动计划(发后下场回复) + 涨粉指标 + 迭代方向(护城河往原创/voice 迁)。"
)
declare -a DIM_FINDINGS=()

if [[ $JSON -eq 0 ]]; then
  ms_section "${INDUSTRY} · Agentic Protocol"
  ms_info "拿到的问题是什么? (输入完后，敲一个空行结束)"
  echo
fi

PROBLEM="$(ms_read_multiline)"
ms_buffer_append "# ${INDUSTRY} — Agentic Protocol Report"
ms_buffer_append ""
ms_buffer_append "_生成时间: $(date '+%Y-%m-%d %H:%M:%S')_"
ms_buffer_append ""
ms_buffer_append "## 问题"
ms_buffer_append ""
ms_buffer_append "$PROBLEM"
ms_buffer_append ""

for i in "${!DIM_TITLES[@]}"; do
  if [[ $JSON -eq 0 ]]; then
    ms_section "${DIM_TITLES[$i]}"
    echo "看什么:    ${DIM_QUESTIONS[$i]}"
    echo "在哪看:    ${DIM_SOURCES[$i]}"
    echo "输出格式:  ${DIM_OUTPUT_FORMATS[$i]}"
    echo
    ms_prompt "你的发现 / 推断 (空行结束):"
    echo
  fi
  finding="$(ms_read_multiline)"
  DIM_FINDINGS+=("$finding")

  ms_buffer_append "## ${DIM_TITLES[$i]}"
  ms_buffer_append ""
  ms_buffer_append "**看什么**: ${DIM_QUESTIONS[$i]}"
  ms_buffer_append ""
  ms_buffer_append "**在哪看**: ${DIM_SOURCES[$i]}"
  ms_buffer_append ""
  ms_buffer_append "**发现**:"
  ms_buffer_append ""
  ms_buffer_append "$finding"
  ms_buffer_append ""
done

if [[ $JSON -eq 1 ]]; then
  args=("problem" "$PROBLEM")
  for i in "${!DIM_TITLES[@]}"; do
    args+=("dim_${i}_title" "${DIM_TITLES[$i]}")
    args+=("dim_${i}_finding" "${DIM_FINDINGS[$i]}")
  done
  ms_emit_json "${args[@]}"
elif [[ $DRY_RUN -eq 1 ]]; then
  ms_emit_md_stdout
else
  ms_emit_md_file "agentic-protocol"
fi
