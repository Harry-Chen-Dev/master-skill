#!/usr/bin/env bash
# agentic.sh — Agentic Protocol CLI for 红队渗透 / 攻防 — 受授权的红队作业者 + 渗透测试工程师 + 攻击型安全顾问的认知操作系统 (侦察 OSINT / 外网渗透 / 内网 AD 渗透 BloodHound + Kerberoasting + ADCS 利用 + 横向移动 / Web 应用渗透 OWASP WSTG / 移动 OWASP MASTG / 云渗透 AWS Azure GCP IAM 路径 + 容器逃逸 + K8s / C2 操作 Cobalt Strike Sliver Mythic Havoc + OPSEC / 初始访问 + AV EDR 绕过 (仅授权场景) / 无线 RF / 物理社工 / 报告与整改 / 框架 MITRE ATT&CK + D3FEND + PTES + OSSTMM + NIST 800-115 + Kill Chain / 法律伦理 CFAA + 网络安全法 + 刑法 285 286 + 数据安全法 + GDPR + 授权书 + 范围 + 交战规则 — 不含 黑产 / 未授权攻击 / 大规模 exploitation / 供应链投毒 / 未授权 DoS — 这是 重罪 + 行业封杀 + 律师吊销, 本 skill 严守 authorized-only 边界 — 也不含 蓝队 SOC + 恶意软件 即服务 / 僵尸网络 / 勒索软件作者 — 这是 cybercrime 不是 红队).
# Auto-generated by cli_writer.py. Source: synthesis.md Section 9.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=../lib/common.sh
source "${SCRIPT_DIR}/../lib/common.sh"

INDUSTRY="红队渗透 / 攻防 — 受授权的红队作业者 + 渗透测试工程师 + 攻击型安全顾问的认知操作系统 (侦察 OSINT / 外网渗透 / 内网 AD 渗透 BloodHound + Kerberoasting + ADCS 利用 + 横向移动 / Web 应用渗透 OWASP WSTG / 移动 OWASP MASTG / 云渗透 AWS Azure GCP IAM 路径 + 容器逃逸 + K8s / C2 操作 Cobalt Strike Sliver Mythic Havoc + OPSEC / 初始访问 + AV EDR 绕过 (仅授权场景) / 无线 RF / 物理社工 / 报告与整改 / 框架 MITRE ATT&CK + D3FEND + PTES + OSSTMM + NIST 800-115 + Kill Chain / 法律伦理 CFAA + 网络安全法 + 刑法 285 286 + 数据安全法 + GDPR + 授权书 + 范围 + 交战规则 — 不含 黑产 / 未授权攻击 / 大规模 exploitation / 供应链投毒 / 未授权 DoS — 这是 重罪 + 行业封杀 + 律师吊销, 本 skill 严守 authorized-only 边界 — 也不含 蓝队 SOC + 恶意软件 即服务 / 僵尸网络 / 勒索软件作者 — 这是 cybercrime 不是 红队)"
DIM_COUNT=7

usage() {
  cat <<EOF
agentic.sh — Agentic Protocol CLI for ${INDUSTRY}

拿到 ${INDUSTRY} 行业的新问题时，按 ${DIM_COUNT} 个研究维度做功课再答。
不直接给答案，而是引导你用资深从业者的研究维度收集信息。

USAGE:
  ./agentic.sh                # interactive walkthrough
  ./agentic.sh --dry-run      # walk through but skip file write
  ./agentic.sh --json         # emit JSON to stdout
  ./agentic.sh --explain      # show backing mental models + sources
  ./agentic.sh --help         # this message
EOF
}

explain() {
  cat <<'EOT'
这个 Agentic Protocol 把这一行人面对新问题的研究维度结构化。

研究维度 (7 个):
  9.1 Authorization + 合规边界 检查
  9.2 Reconnaissance + 资产 mapping + Attack Surface
  9.3 Threat Model + Attack Path Planning (ATT&CK)
  9.4 Tradecraft Selection + OPSEC Planning
  9.5 Detection Co-Design + Purple Team Integration
  9.6 Reporting + Remediation Roadmap
  9.7 Continuous Learning + Community + 法规追踪

背后的心智模型:
  • Authorization-First — 授权先于一切, 范围严于法律: 红队 不是 "我能不能 hack 它", 是 "客户授权我 hack 哪些 / 不授权 hack 哪些 / 边界在哪 / 越界即重罪" — engagement letter + signed SOW
  • Assume Breach — 默认已被攻陷, 起点是 Domain User 不是 No Access: 现代企业 perimeter 早已不可信 (phishing + supply chain + 0-day 任一 都能进), 防御只能 在 "内部" 做 — 因此 红队 默认起点 = "我已经是 一个
  • Attack Path Thinking — 单点漏洞无价值, 链 (chain) 才是 finding: 现代红队 不报 "你有 SQL 注入" — 报 "SQL 注入 → admin token leak → S3 写权限 → Lambda 后门 → cross-account assume role 

来源: synthesis.md Section 9 + Section 1.
EOT
}

DRY_RUN=0; JSON=0
for arg in "$@"; do
  case "$arg" in
    --help|-h) usage; exit 0 ;;
    --explain) explain; exit 0 ;;
    --dry-run) DRY_RUN=1 ;;
    --json) JSON=1 ;;
  esac
done

declare -a DIM_TITLES=(
  "Authorization + 合规边界 检查"
  "Reconnaissance + 资产 mapping + Attack Surface"
  "Threat Model + Attack Path Planning (ATT&CK)"
  "Tradecraft Selection + OPSEC Planning"
  "Detection Co-Design + Purple Team Integration"
  "Reporting + Remediation Roadmap"
  "Continuous Learning + Community + 法规追踪"
)
declare -a DIM_QUESTIONS=(
  "engagement letter + signed SOW + ROE 三件套; in-scope assets + out-of-scope assets + 允许 TTPs + 禁止 TTPs (DoS / brute force lockout / 物理破坏); 业务时间窗 + 维护窗口; 紧急联系人 7×24 + safe word; 客户 注册地 + 数据 所在地 + 执行者 国籍 三重 适用法律 (CFAA / CMA / 网安法 / GDPR / PIPL); 客户 已通知 IT/SOC 高层 (但 一线 unannounced); bug bounty program ROE + safe harbor + 公开 PoC timeline."
  "passive recon (subdomain enum / cert transparency monitor / GitHub secret scan / Shodan / Censys / FOFA / ZoomEye / Hunter / public document leak); active recon (port scan / service fingerprint / 漏洞 fingerprint nuclei / web app crawl / API discovery / GraphQL introspection); 资产 第三方 SaaS + 收购 子公司 (scope 外? + ROE 确认); cloud asset (AWS / Azure / GCP account + sub + project + resource); mobile app (Play Store / App Store + 内部 distribution); ICS/OT / wireless / 物理."
  "客户 industry threat profile (financial / healthcare / government / SaaS / industrial — 不同 industry 主要 threat actor 不同); 客户 已知 historical incident + dump / breach 历史; ATT&CK technique 优先级 (per industry + per attacker capability — APT 国家级 vs criminal vs insider); 客户 crown jewel (PII / financial / IP / OT controller) 隔离 状态; existing detection coverage (EDR vendor / SIEM / Sysmon / Sigma rule library); D3FEND defense mapping."
  "客户 EDR vendor (CrowdStrike Falcon / Microsoft Defender + Defender for Endpoint / SentinelOne / Carbon Black / Elastic / Sophos / Cybereason); SIEM (Splunk + KQL / Sentinel + Elastic / Sumo Logic); 网络 detection (Zeek / Suricata / Snort / Palo Alto / Check Point + Cortex XDR); 客户 历史 EDR alert pattern; 公开 EDR bypass research (community); 自研 vs 公开 工具 决策 (mature EDR 必 custom obfuscation); C2 framework + malleable profile + Domain Fronting + categorized aged domain; payload obfuscation (Invoke-Obfuscation / ConfuserEx / 自研 packer); persistence + cleanup plan."
  "客户 detection engineering team 存在 + maturity; existing Sigma / KQL / Splunk SPL rule library; Atomic Red Team / CALDERA 已部署?; SOC analyst skill level; 客户 是否 接受 purple-team-first vs unannounced; 监管型 (TIBER-EU / CBEST / monetary authority) requirement; 红蓝 cross-training 历史."
  "客户 audience (CISO / CFO / Legal / Board vs Detection Engineering / SOC vs Developer / DevOps); 报告 audience-tier 结构 (Executive Summary + Technical Findings + Detection Guidance + Appendix); CVSS + ATT&CK ID + EPSS + KEV + business impact 综合 severity; remediation effort + 优先级 (P0 / P1 / P2) + retest plan + 长期 roadmap; 报告 模板 (PlexTrac / Dradis / 自研); 客户 internal vulnerability management workflow."
  "新 EDR bypass research (community 月度) + 新 ATT&CK technique (季度) + 新 ADCS ESC chain (年度) + 新 cloud attack vector + 新 cert + program update + 法规 update (网安法 + GDPR + NIS2 + PIPL + state law); CTF + lab + 培训 (HackTheBox / TryHackMe / PortSwigger / OffSec PG / SANS NetWars) 持续 training; 行业 podcast + newsletter + Discord + Mastodon 追踪; 顶级 conference 录像 (DEF CON / Black Hat / OffensiveCon / TROOPERS / CCC / x33fcon)."
)
declare -a DIM_SOURCES=(
  "engagement letter (client + 律师 review) + ROE (客户 高层 签字 + 律师 review) + 客户 法律部 + 律师 (engagement-specific counsel) + 公开法律 (Cornell Law CFAA 18 USC 1030 [auto-verified .edu] + 中国 npc.gov.cn 刑法 + 网安法 [auto-verified .gov.cn] + EU GDPR Eur-Lex [surrogate primary]) + bug bounty platform program page (HackerOne / Bugcrowd / Intigriti / YesWeHack / Synack)."
  "subfinder + amass + chaos + crt.sh + Shodan + Censys + FOFA + Hunter + ZoomEye + GitHub TruffleHog + WhoisXML + cloud asset (AWS Resource Explorer / Azure Resource Graph / GCP Cloud Asset Inventory) + custom cert transparency monitor + JS analysis ParamSpider; 第三方 SaaS (Have I Been Pwned + DNS records + email tooling)."
  "MITRE ATT&CK [vendor docs 协会 + 教材, surrogate_primary] + MITRE D3FEND + Mandiant / Microsoft / CrowdStrike / Google TAG threat intel reports (vendor docs 厂商研究) + CISA Cybersecurity Advisories (auto-verified .gov) + CSC Critical Security Controls + 客户 internal threat model + 客户 incident history; CISA KEV catalog (auto-verified .gov); EPSS first.org (vendor docs 协会)."
  "vendor official EDR documentation (CrowdStrike + Microsoft + SentinelOne 官方) + community EDR bypass research (SpecterOps + ired.team + adsecurity + harmj0y + hexacorn 等 surrogate primary 作者一手) + 公开 GitHub C2 framework docs (Cobalt Strike + Sliver + Mythic + Havoc) + DEF CON / Black Hat / OffensiveCon talks (vendor docs 会议) + Pwn2Own + ZDI threat intelligence; vx-underground (community)."
  "客户 detection engineering team interview + SOC tour + 看 existing rule library (Sigma + KQL repo) + Atomic Red Team [GitHub vendor docs] + CALDERA [Mandiant vendor docs] + Vectr + Scythe 平台; MITRE ATT&CK + D3FEND mapping; ECB TIBER-EU + Bank of England CBEST 监管 framework; CISA Joint Cybersecurity Advisories."
  "客户 audience interview + 客户 internal vulnerability management process + 公开 报告 模板 (PTES report template + OffSec OSCP report sample + SANS report 教材) + CVSS Calculator (first.org) + CISA KEV + EPSS first.org + ATT&CK + D3FEND mapping."
  "tldr;sec (Clint Gibler newsletter) + Risky Biz (Patrick Gray podcast + news) + The Cyberwire + Darknet Diaries + BloodHound Slack + Mythic Discord + infosec.exchange (Mastodon) + Twitter sec 圈; CISA + NIST CSRC + NCSC UK + ENISA + CNCERT/CC; DEF CON / Black Hat / OffensiveCon / TROOPERS / CCC media (vendor docs 会议); ATT&CK release notes + OWASP WSTG/MASTG release; 中国 anquanke + freebuf + kanxue + paper.seebug + xz.aliyun + tttang."
)
declare -a DIM_OUTPUT_FORMATS=(
  "Authorization checklist + scope 表 (IP / asset / 时间窗 / 允许 TTPs / 禁止 TTPs) + emergency contact 7×24 + safe word + 法律 适用 三重检查 + 客户 通知 confirm + bug bounty ROE 严守 confirm + 任何越界 STOP + 上报 上层 SOP."
  "资产清单 (in-scope + out-of-scope + 边界 ambiguous) + technology stack (web framework + database + cloud + EDR / SIEM detected) + initial attack surface (open service + 漏洞 fingerprint + chain candidate) + 越界 STOP list (out-of-scope asset 发现 立即 上报)."
  "Threat model + ATT&CK technique 优先级 表 + crown jewel 隔离 评估 + existing detection coverage gap (per ATT&CK Tactic) + attack path candidate chain (entry → lateral → priv esc → crown jewel) + D3FEND mapping (每 ATT&CK 配 对应 defense pattern + 评估 客户 implement vs 缺)."
  "Tradecraft 选型 表 (per attack path stage — initial access / execution / persistence / priv esc / lateral / exfil 各阶段 EDR-evasive technique 选择) + OPSEC profile (custom malleable C2 + sleep / jitter + domain + redirector + payload obfuscation) + cleanup plan (engagement 结束 cleanup all artifact) + abort criteria (何时 STOP + 上报 客户)."
  "Detection co-design plan (per attack path stage — 哪 technique 客户 detect / 哪 silent / 哪 误报) + Sigma rule + KQL query for missed detection + 复盘 workshop schedule + 红蓝 cross-training 建议 + 监管型 (TIBER-EU / CBEST) compliance check."
  "三层 报告 (Executive Summary 1-2 页 / Technical Findings 50-200 页 / Appendix raw artifact) + 每 finding 含 CVSS + ATT&CK + EPSS + business impact + repro + remediation + retest + detection guidance (Sigma + KQL) + 优先级 P0/P1/P2 + roadmap timeline + retest 14/30 day scheduled."
  "个人 / 团队 learning roadmap + 每月 community digest + 季度 cert + 培训 计划 + 监管 update 追踪 + 关键 conference 录像 review + 自研 lab + tooling 持续 投入."
)
declare -a DIM_FINDINGS=()

if [[ $JSON -eq 0 ]]; then
  ms_section "${INDUSTRY} · Agentic Protocol"
  ms_info "拿到的问题是什么? (输入完后，敲一个空行结束)"
  echo
fi

PROBLEM="$(ms_read_multiline)"
ms_buffer_append "# ${INDUSTRY} — Agentic Protocol Report"
ms_buffer_append ""
ms_buffer_append "_生成时间: $(date '+%Y-%m-%d %H:%M:%S')_"
ms_buffer_append ""
ms_buffer_append "## 问题"
ms_buffer_append ""
ms_buffer_append "$PROBLEM"
ms_buffer_append ""

for i in "${!DIM_TITLES[@]}"; do
  if [[ $JSON -eq 0 ]]; then
    ms_section "${DIM_TITLES[$i]}"
    echo "看什么:    ${DIM_QUESTIONS[$i]}"
    echo "在哪看:    ${DIM_SOURCES[$i]}"
    echo "输出格式:  ${DIM_OUTPUT_FORMATS[$i]}"
    echo
    ms_prompt "你的发现 / 推断 (空行结束):"
    echo
  fi
  finding="$(ms_read_multiline)"
  DIM_FINDINGS+=("$finding")

  ms_buffer_append "## ${DIM_TITLES[$i]}"
  ms_buffer_append ""
  ms_buffer_append "**看什么**: ${DIM_QUESTIONS[$i]}"
  ms_buffer_append ""
  ms_buffer_append "**在哪看**: ${DIM_SOURCES[$i]}"
  ms_buffer_append ""
  ms_buffer_append "**发现**:"
  ms_buffer_append ""
  ms_buffer_append "$finding"
  ms_buffer_append ""
done

if [[ $JSON -eq 1 ]]; then
  args=("problem" "$PROBLEM")
  for i in "${!DIM_TITLES[@]}"; do
    args+=("dim_${i}_title" "${DIM_TITLES[$i]}")
    args+=("dim_${i}_finding" "${DIM_FINDINGS[$i]}")
  done
  ms_emit_json "${args[@]}"
elif [[ $DRY_RUN -eq 1 ]]; then
  ms_emit_md_stdout
else
  ms_emit_md_file "agentic-protocol"
fi
