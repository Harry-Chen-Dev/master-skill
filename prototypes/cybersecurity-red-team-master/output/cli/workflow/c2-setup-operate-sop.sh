#!/usr/bin/env bash
# c2-setup-operate-sop.sh — C2 Setup + Operate SOP SOP walkthrough for 红队渗透 / 攻防 — 受授权的红队作业者 + 渗透测试工程师 + 攻击型安全顾问的认知操作系统 (侦察 OSINT / 外网渗透 / 内网 AD 渗透 BloodHound + Kerberoasting + ADCS 利用 + 横向移动 / Web 应用渗透 OWASP WSTG / 移动 OWASP MASTG / 云渗透 AWS Azure GCP IAM 路径 + 容器逃逸 + K8s / C2 操作 Cobalt Strike Sliver Mythic Havoc + OPSEC / 初始访问 + AV EDR 绕过 (仅授权场景) / 无线 RF / 物理社工 / 报告与整改 / 框架 MITRE ATT&CK + D3FEND + PTES + OSSTMM + NIST 800-115 + Kill Chain / 法律伦理 CFAA + 网络安全法 + 刑法 285 286 + 数据安全法 + GDPR + 授权书 + 范围 + 交战规则 — 不含 黑产 / 未授权攻击 / 大规模 exploitation / 供应链投毒 / 未授权 DoS — 这是 重罪 + 行业封杀 + 律师吊销, 本 skill 严守 authorized-only 边界 — 也不含 蓝队 SOC + 恶意软件 即服务 / 僵尸网络 / 勒索软件作者 — 这是 cybercrime 不是 红队).
# Auto-generated by cli_writer.py. Source: research/03-workflows.md.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/../lib/common.sh"

INDUSTRY="红队渗透 / 攻防 — 受授权的红队作业者 + 渗透测试工程师 + 攻击型安全顾问的认知操作系统 (侦察 OSINT / 外网渗透 / 内网 AD 渗透 BloodHound + Kerberoasting + ADCS 利用 + 横向移动 / Web 应用渗透 OWASP WSTG / 移动 OWASP MASTG / 云渗透 AWS Azure GCP IAM 路径 + 容器逃逸 + K8s / C2 操作 Cobalt Strike Sliver Mythic Havoc + OPSEC / 初始访问 + AV EDR 绕过 (仅授权场景) / 无线 RF / 物理社工 / 报告与整改 / 框架 MITRE ATT&CK + D3FEND + PTES + OSSTMM + NIST 800-115 + Kill Chain / 法律伦理 CFAA + 网络安全法 + 刑法 285 286 + 数据安全法 + GDPR + 授权书 + 范围 + 交战规则 — 不含 黑产 / 未授权攻击 / 大规模 exploitation / 供应链投毒 / 未授权 DoS — 这是 重罪 + 行业封杀 + 律师吊销, 本 skill 严守 authorized-only 边界 — 也不含 蓝队 SOC + 恶意软件 即服务 / 僵尸网络 / 勒索软件作者 — 这是 cybercrime 不是 红队)"
WORKFLOW="C2 Setup + Operate SOP"

usage() {
  cat <<EOF
c2-setup-operate-sop.sh — Walk through "${WORKFLOW}" SOP for ${INDUSTRY}

逐步走完这个工作流的入门 SOP, 然后做失败模式自检, 输出走查报告。

USAGE:
  ./c2-setup-operate-sop.sh             # interactive
  ./c2-setup-operate-sop.sh --dry-run
  ./c2-setup-operate-sop.sh --json
  ./c2-setup-operate-sop.sh --explain
  ./c2-setup-operate-sop.sh --help
EOF
}

explain() {
  cat <<'EOT'
这个 SOP 走查脚本帮你完成 "C2 Setup + Operate SOP" 工作流。

入门 SOP 共 8 步, 失败模式自检 0 项。

相关心智模型:
  • Authorization-First — 授权先于一切, 范围严于法律: 红队 不是 "我能不能 hack 它", 是 "客户授权我 hack 哪些 / 不授权 hack 哪些 / 边界在哪 / 越界即重罪" — engagement letter + signed SOW
  • Assume Breach — 默认已被攻陷, 起点是 Domain User 不是 No Access: 现代企业 perimeter 早已不可信 (phishing + supply chain + 0-day 任一 都能进), 防御只能 在 "内部" 做 — 因此 红队 默认起点 = "我已经是 一个

来源: research/03-workflows.md.
EOT
}

DRY_RUN=0; JSON=0
for arg in "$@"; do
  case "$arg" in
    --help|-h) usage; exit 0 ;;
    --explain) explain; exit 0 ;;
    --dry-run) DRY_RUN=1 ;;
    --json) JSON=1 ;;
  esac
done

declare -a SOP_STEPS=(
  "**Phase 0 — Infrastructure plan**: 设计 redirector (Nginx / Apache mod_rewrite / HAProxy) → C2 server 二层架构, CDN front (Cloudflare / Fastly / Azure Front Door), categorization aged domain (购 30+ days 老域名 + Google SafeBrowsing / OpenDNS / Cisco Talos / VirusTotal 分类查), Let's Encrypt cert."
  "**Phase 1 — C2 server 部署**: Cobalt Strike teamserver (Java) 或 Sliver (Go) 或 Mythic (Docker compose) 部署在 隔离 VPS, 配置 listener (HTTP / HTTPS / DNS / SMB pipe / TCP)."
  "**Phase 2 — Malleable C2 profile (CS) / extensible config**: 写 .profile 文件指定 HTTP header + URI + 数据 transform + sleep + jitter + DNS profile, 与 redirector + CDN 配合做 traffic camouflage."
  "**Phase 3 — Payload generation**: 生成 stager / stageless beacon (CS) / implant (Sliver: sliver generate --mtls / Mythic: payload type Apollo/Poseidon/Athena), 配合 obfuscation (ConfuserEx / Donut / sRDI / Garble for Go)."
  "**Phase 4 — Initial delivery**: phishing / 物理 USB / supply chain (在 ROE 内) deliver payload."
  "**Phase 5 — Beacon callback + tasking**: callback 后立即 sleep 调高 + situation awareness (whoami / hostname / domain / AV/EDR 检测)."
  "**Phase 6 — Post-ex**: 按 ATT&CK tactics 走 (Discovery → Cred Access → Lateral → Collection → Exfil), 每步 OPSEC 评估."
  "**Phase 7 — 离场 cleanup**: kill all beacons + 通知客户 + infrastructure 解构 (域名 + cert 不复用) + log archive."
)
declare -a FAILURE_MODES=(

)
declare -a STEP_STATUS=()
declare -a STEP_NOTES=()
declare -a FAIL_CHECK=()

ms_section "${INDUSTRY} · ${WORKFLOW} 走查"
ms_info "逐步走完 ${#SOP_STEPS[@]} 步入门 SOP。每步可标 done / skipped / blocked。"
echo

for i in "${!SOP_STEPS[@]}"; do
  ms_section "Step $((i+1))/${#SOP_STEPS[@]}: ${SOP_STEPS[$i]}"
  ms_prompt "状态? [d=done / s=skipped / b=blocked]"
  read -r status
  STEP_STATUS+=("${status:-d}")
  case "${status:-d}" in
    s) ms_prompt "跳过原因 (一句话):"; read -r note ;;
    b) ms_prompt "blocker (一句话):"; read -r note ;;
    *) note="" ;;
  esac
  STEP_NOTES+=("$note")
done

if [[ ${#FAILURE_MODES[@]} -gt 0 ]]; then
  ms_section "失败模式自检"
  for fm in "${FAILURE_MODES[@]}"; do
    if ms_confirm "你是否避免了 — '$fm' ?"; then
      FAIL_CHECK+=("✓ $fm")
    else
      FAIL_CHECK+=("✗ $fm")
    fi
  done
fi

ms_buffer_append "# ${INDUSTRY} — ${WORKFLOW} 走查报告"
ms_buffer_append ""
ms_buffer_append "_生成时间: $(date '+%Y-%m-%d %H:%M:%S')_"
ms_buffer_append ""
ms_buffer_append "## SOP 步骤"
ms_buffer_append ""
for i in "${!SOP_STEPS[@]}"; do
  status_icon="?"
  case "${STEP_STATUS[$i]}" in
    d|D) status_icon="✓" ;;
    s|S) status_icon="↷" ;;
    b|B) status_icon="✗" ;;
  esac
  if [[ -n "${STEP_NOTES[$i]}" ]]; then
    ms_buffer_append "$((i+1)). [${status_icon}] ${SOP_STEPS[$i]} — ${STEP_NOTES[$i]}"
  else
    ms_buffer_append "$((i+1)). [${status_icon}] ${SOP_STEPS[$i]}"
  fi
done
ms_buffer_append ""
if [[ ${#FAIL_CHECK[@]} -gt 0 ]]; then
  ms_buffer_append "## 失败模式自检"
  ms_buffer_append ""
  for line in "${FAIL_CHECK[@]}"; do
    ms_buffer_append "- $line"
  done
  ms_buffer_append ""
fi

if [[ $JSON -eq 1 ]]; then
  ms_emit_json \
    "workflow" "${WORKFLOW}" \
    "step_count" "${#SOP_STEPS[@]}" \
    "failure_check_count" "${#FAILURE_MODES[@]}"
elif [[ $DRY_RUN -eq 1 ]]; then
  ms_emit_md_stdout
else
  ms_emit_md_file "c2-setup-operate-sop-walkthrough"
fi
