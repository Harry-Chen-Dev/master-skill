#!/usr/bin/env bash
# topic-1.sh — 客户 决策助手 for 红队渗透 / 攻防 — 受授权的红队作业者 + 渗透测试工程师 + 攻击型安全顾问的认知操作系统 (侦察 OSINT / 外网渗透 / 内网 AD 渗透 BloodHound + Kerberoasting + ADCS 利用 + 横向移动 / Web 应用渗透 OWASP WSTG / 移动 OWASP MASTG / 云渗透 AWS Azure GCP IAM 路径 + 容器逃逸 + K8s / C2 操作 Cobalt Strike Sliver Mythic Havoc + OPSEC / 初始访问 + AV EDR 绕过 (仅授权场景) / 无线 RF / 物理社工 / 报告与整改 / 框架 MITRE ATT&CK + D3FEND + PTES + OSSTMM + NIST 800-115 + Kill Chain / 法律伦理 CFAA + 网络安全法 + 刑法 285 286 + 数据安全法 + GDPR + 授权书 + 范围 + 交战规则 — 不含 黑产 / 未授权攻击 / 大规模 exploitation / 供应链投毒 / 未授权 DoS — 这是 重罪 + 行业封杀 + 律师吊销, 本 skill 严守 authorized-only 边界 — 也不含 蓝队 SOC + 恶意软件 即服务 / 僵尸网络 / 勒索软件作者 — 这是 cybercrime 不是 红队).
# Auto-generated by cli_writer.py. Source: synthesis.md Section 2 (Playbook).
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/../lib/common.sh"

INDUSTRY="红队渗透 / 攻防 — 受授权的红队作业者 + 渗透测试工程师 + 攻击型安全顾问的认知操作系统 (侦察 OSINT / 外网渗透 / 内网 AD 渗透 BloodHound + Kerberoasting + ADCS 利用 + 横向移动 / Web 应用渗透 OWASP WSTG / 移动 OWASP MASTG / 云渗透 AWS Azure GCP IAM 路径 + 容器逃逸 + K8s / C2 操作 Cobalt Strike Sliver Mythic Havoc + OPSEC / 初始访问 + AV EDR 绕过 (仅授权场景) / 无线 RF / 物理社工 / 报告与整改 / 框架 MITRE ATT&CK + D3FEND + PTES + OSSTMM + NIST 800-115 + Kill Chain / 法律伦理 CFAA + 网络安全法 + 刑法 285 286 + 数据安全法 + GDPR + 授权书 + 范围 + 交战规则 — 不含 黑产 / 未授权攻击 / 大规模 exploitation / 供应链投毒 / 未授权 DoS — 这是 重罪 + 行业封杀 + 律师吊销, 本 skill 严守 authorized-only 边界 — 也不含 蓝队 SOC + 恶意软件 即服务 / 僵尸网络 / 勒索软件作者 — 这是 cybercrime 不是 红队)"
TOPIC="客户"

usage() {
  cat <<EOF
topic-1.sh — ${TOPIC} 决策助手 for ${INDUSTRY}

基于 7 条 playbook 规则的交互式决策评估。
你描述情景, 工具帮你判断哪条规则适用, 并给推荐。

USAGE:
  ./topic-1.sh             # interactive
  ./topic-1.sh --dry-run   # walk through but no file
  ./topic-1.sh --json      # JSON to stdout
  ./topic-1.sh --explain   # show backing playbook rules + mental models
  ./topic-1.sh --help      # this message
EOF
}

explain() {
  cat <<'EOT'
这个决策助手把 客户 主题下的 playbook 规则组合成交互树。

基于 7 条规则:
  • 如果 客户 未提供 engagement letter + signed SOW + ROE 三件套 → 则 : 则 STOP — 不开 box, 不 scan, 不 OSINT; 律师 review + 客户高层 签字 + 紧急
  • 如果 engagement ROE 不含 DoS / DDoS / brute force lockout / 物理破坏 /  → 则 : 则 严守不做 — 即使 attack path 必经 DoS, STOP + 上报 客户 + 申请 ROE 扩展; 
  • 如果 发现 越界资产 (scope 之外) → 则 : 则 STOP + 立即上报 客户 (写 邮件 + Slack 双轨), 不动 + 不 fingerprint + 不
  • 如果 发现 critical (CVSS 9.x + ATT&CK pre-Impact chain 完成) → 则 : 则 STOP exploit, 优先 评估业务影响 + 沟通客户 是否 继续 weaponize; 不可 defau
  • 如果 在 中国境内 渗透 (即使有合同) → 则 : 则 客户 必须 已完成 等保备案 + 委托 资质 渗透公司 (公安部第三研究所 / 中国信安测评中心 / 国家互联网
  • 如果 选 公开 工具 (Mimikatz / SharpHound / PsExec / NetExec / Cobalt S → 则 : 则 不直接 deploy — 必 自编译 + 加壳 + obfuscate (Invoke-Obfuscation 
  • 如果 engagement 含 EDR bypass + payload 开发 + AMSI/ETW bypass → 则 : 则 技术 公开 (Microsoft 自己 ATT&CK 都有教学), 但 部署 必 在 engagement le

相关心智模型:
  • Authorization-First — 授权先于一切, 范围严于法律: 红队 不是 "我能不能 hack 它", 是 "客户授权我 hack 哪些 / 不授权 hack 哪些 / 边界在哪 / 越界即重罪" — engagement letter + signed SOW
  • Assume Breach — 默认已被攻陷, 起点是 Domain User 不是 No Access: 现代企业 perimeter 早已不可信 (phishing + supply chain + 0-day 任一 都能进), 防御只能 在 "内部" 做 — 因此 红队 默认起点 = "我已经是 一个

来源: synthesis.md Section 2.
EOT
}

DRY_RUN=0; JSON=0
for arg in "$@"; do
  case "$arg" in
    --help|-h) usage; exit 0 ;;
    --explain) explain; exit 0 ;;
    --dry-run) DRY_RUN=1 ;;
    --json) JSON=1 ;;
  esac
done

declare -a RULE_CONDITIONS=(
  "客户 未提供 engagement letter + signed SOW + ROE 三件套"
  "engagement ROE 不含 DoS / DDoS / brute force lockout / 物理破坏 / 社工"
  "发现 越界资产 (scope 之外)"
  "发现 critical (CVSS 9.x + ATT&CK pre-Impact chain 完成)"
  "在 中国境内 渗透 (即使有合同)"
  "选 公开 工具 (Mimikatz / SharpHound / PsExec / NetExec / Cobalt Strike default)"
  "engagement 含 EDR bypass + payload 开发 + AMSI/ETW bypass"
)
declare -a RULE_ACTIONS=(
  ": 则 STOP — 不开 box, 不 scan, 不 OSINT; 律师 review + 客户高层 签字 + 紧急联系人 + safe word 全到位 才启动"
  ": 则 严守不做 — 即使 attack path 必经 DoS, STOP + 上报 客户 + 申请 ROE 扩展; default ROE 不含 DoS, 必须 显式 客户 签字 + 接受 短暂业务中断 + 维护窗口"
  ": 则 STOP + 立即上报 客户 (写 邮件 + Slack 双轨), 不动 + 不 fingerprint + 不 enumerate; 等 客户 决定 加入 scope (新 SOW) 或 confirm 越界后撤; 任何越界即犯罪 (CFAA 不区分 故意 vs 无意)"
  ": 则 STOP exploit, 优先 评估业务影响 + 沟通客户 是否 继续 weaponize; 不可 default 直接 weaponize; 客户 决定 (a) 立即修复, 不动 (b) 拿到 PoC 即停 (c) 继续测 detection"
  ": 则 客户 必须 已完成 等保备案 + 委托 资质 渗透公司 (公安部第三研究所 / 中国信安测评中心 / 国家互联网应急中心 CNCERT / 启明星辰 / 绿盟 / 奇安信 / 360 / 深信服 / 安恒); 私 individual 顾问 即使 合同 灰色 (网络安全法 27 + 数据安全法 + 刑法 285); 推荐 客户 先 等保备案 + 公安部 报备 + 委托 资质公司"
  ": 则 不直接 deploy — 必 自编译 + 加壳 + obfuscate (Invoke-Obfuscation / ConfuserEx / 自研 BOF / 自定义 malleable profile); 直接 GitHub binary 99% trip on Defender / EDR (业内 共识 2020+); 即使 OPSEC 不严要求, 现代 EDR 报警速度快 (秒级), 客户 SOC 拿到报警 就触发 incident response, 浪费 engagement 时间"
  ": 则 技术 公开 (Microsoft 自己 ATT&CK 都有教学), 但 部署 必 在 engagement letter 显式 authorization 范围内; 知识 vs 行为 严区分; 不外传 客户 specific bypass (毁 客户 detection 投资); 公开 PoC / blog 等 engagement 结束 + 客户 patch + 90 day 后 才考虑"
)
declare -a APPLIED_RULES=()

ms_section "${INDUSTRY} · ${TOPIC} 决策评估"
ms_info "本工具基于 ${#RULE_CONDITIONS[@]} 条 playbook 规则评估你的情景。"
echo

ms_prompt "请描述当前情景 (空行结束):"
echo
SITUATION="$(ms_read_multiline)"

for i in "${!RULE_CONDITIONS[@]}"; do
  ms_section "Rule $((i+1))/${#RULE_CONDITIONS[@]}"
  echo "适用情景: ${RULE_CONDITIONS[$i]}"
  echo "推荐做法: ${RULE_ACTIONS[$i]}"
  if ms_confirm "这条规则 fit 你当前情景吗?"; then
    APPLIED_RULES+=("$((i+1))")
    ms_ok "适用 rule $((i+1))"
  fi
done

ms_buffer_append "# ${INDUSTRY} — ${TOPIC} 决策报告"
ms_buffer_append ""
ms_buffer_append "_生成时间: $(date '+%Y-%m-%d %H:%M:%S')_"
ms_buffer_append ""
ms_buffer_append "## 情景"
ms_buffer_append ""
ms_buffer_append "$SITUATION"
ms_buffer_append ""
ms_buffer_append "## 适用规则"
ms_buffer_append ""
if [[ ${#APPLIED_RULES[@]} -eq 0 ]]; then
  ms_buffer_append "(无适用 playbook 规则 — 此情景在已知 OS 之外, 建议回到 SKILL.md 心智模型层重新分析)"
else
  for ridx in "${APPLIED_RULES[@]}"; do
    i=$((ridx-1))
    ms_buffer_append "- **情景**: ${RULE_CONDITIONS[$i]} → **做法**: ${RULE_ACTIONS[$i]}"
  done
fi
ms_buffer_append ""

if [[ $JSON -eq 1 ]]; then
  ms_emit_json \
    "topic" "${TOPIC}" \
    "situation" "${SITUATION}" \
    "applied_rules_count" "${#APPLIED_RULES[@]}"
elif [[ $DRY_RUN -eq 1 ]]; then
  ms_emit_md_stdout
else
  ms_emit_md_file "topic-1-decision"
fi
