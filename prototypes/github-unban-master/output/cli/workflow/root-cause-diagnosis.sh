#!/usr/bin/env bash
# root-cause-diagnosis.sh — 封号原因深度诊断 (Root Cause Diagnosis) SOP walkthrough for GitHub 解封 — 账号封禁/限制/恢复的领域操作系统。覆盖：(a) GitHub 官方政策体系 (TOS / 可接受使用政策 / Trust & Safety 执行模式 / 公开申诉路径 / 受制裁地区解读)；(b) 封号原因诊断学 (spam / abuse / 制裁误标 / 2FA 丢失 / ToS 违规 / 账号劫持误判 / ban evasion 等子类型 + 症状→原因映射 + 自我分诊压测)；(c) 申诉实操 craft (写一封能让 T&S 快速判误伤的英文申诉信 + 升级阶梯 + 法务介入边界 + Don'ts)；(d) 制裁与合规硬边界 (OFAC SDN 名单 / 哪些国家或地区真不可达 / VPN 误判路径 / 中国大陆不是美国制裁地区的反复重申)；(e) 中国大陆开发者特别处境 (sanctioned region 误标 + SMS 验证不支持 +86/+852 + Gitee/GitLab/Codeberg/JihuLab 备用迁移 + 私有自托管 Gitea/Forgejo)；(f) 真实案例库 (~30+ 公开案例分类拆解：误伤 / 真违规 / 制裁误标三类各自赔率与申诉成功路径)。伦理锚绝不软化：不教 ban evasion / 不教撒谎式申诉 / 真违规承认改进 / 丢 2FA 无恢复因子则诚实告知 / 中国制裁误标靠申诉不靠换 VPN / 申诉 6 个月窗口必算清 / ~3% 真违规恢复 vs ~79% 误伤申诉成功率分层标。.
# Auto-generated by cli_writer.py. Source: research/03-workflows.md.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "${SCRIPT_DIR}/../lib/common.sh"

INDUSTRY="GitHub 解封 — 账号封禁/限制/恢复的领域操作系统。覆盖：(a) GitHub 官方政策体系 (TOS / 可接受使用政策 / Trust & Safety 执行模式 / 公开申诉路径 / 受制裁地区解读)；(b) 封号原因诊断学 (spam / abuse / 制裁误标 / 2FA 丢失 / ToS 违规 / 账号劫持误判 / ban evasion 等子类型 + 症状→原因映射 + 自我分诊压测)；(c) 申诉实操 craft (写一封能让 T&S 快速判误伤的英文申诉信 + 升级阶梯 + 法务介入边界 + Don'ts)；(d) 制裁与合规硬边界 (OFAC SDN 名单 / 哪些国家或地区真不可达 / VPN 误判路径 / 中国大陆不是美国制裁地区的反复重申)；(e) 中国大陆开发者特别处境 (sanctioned region 误标 + SMS 验证不支持 +86/+852 + Gitee/GitLab/Codeberg/JihuLab 备用迁移 + 私有自托管 Gitea/Forgejo)；(f) 真实案例库 (~30+ 公开案例分类拆解：误伤 / 真违规 / 制裁误标三类各自赔率与申诉成功路径)。伦理锚绝不软化：不教 ban evasion / 不教撒谎式申诉 / 真违规承认改进 / 丢 2FA 无恢复因子则诚实告知 / 中国制裁误标靠申诉不靠换 VPN / 申诉 6 个月窗口必算清 / ~3% 真违规恢复 vs ~79% 误伤申诉成功率分层标。"
WORKFLOW="封号原因深度诊断 (Root Cause Diagnosis)"

usage() {
  cat <<EOF
root-cause-diagnosis.sh — Walk through "${WORKFLOW}" SOP for ${INDUSTRY}

逐步走完这个工作流的入门 SOP, 然后做失败模式自检, 输出走查报告。

USAGE:
  ./root-cause-diagnosis.sh             # interactive
  ./root-cause-diagnosis.sh --dry-run
  ./root-cause-diagnosis.sh --json
  ./root-cause-diagnosis.sh --explain
  ./root-cause-diagnosis.sh --help
EOF
}

explain() {
  cat <<'EOT'
这个 SOP 走查脚本帮你完成 "封号原因深度诊断 (Root Cause Diagnosis)" 工作流。

入门 SOP 共 8 步, 失败模式自检 0 项。

来源: research/03-workflows.md.
EOT
}

DRY_RUN=0; JSON=0
for arg in "$@"; do
  case "$arg" in
    --help|-h) usage; exit 0 ;;
    --explain) explain; exit 0 ;;
    --dry-run) DRY_RUN=1 ;;
    --json) JSON=1 ;;
  esac
done

declare -a SOP_STEPS=(
  "审查 GitHub 通知: 仔细阅读封号邮件/页面提示中的每一个关键词 — \"spam\" / \"abuse\" / \"trade controls\" / \"malware\" / \"impersonation\" / \"DMCA\" / \"disruption\" / \"security\" 每个词指向不同原因和不同申诉策略 [T03-S022] [T03-S035]"
  "回溯近 30 天行为: 是否 mass fork 了大量仓库? 是否批量 star 了大量项目? 是否在短时间内创建了大量 Issues/PRs (可能被判定为 spam)? 是否使用了 API 自动化做大量操作 (超过 rate limit)? 是否加入了 \"互 star\" / \"star exchange\" 平台? [T03-S022] [T03-S035]"
  "检查 IP/VPN 地理位置: 近期是否使用过 VPN? VPN 出口 IP 是否在制裁区 (克里米亚 / 古巴 / 朝鲜 / 伊朗虽已有 GL D-2 但某些操作仍受限)? GitHub 通过 IP 地址和支付历史判定地理位置, 不基于国籍 [T03-S002]; 中国大陆不是制裁区, 但如果使用了伊朗/俄罗斯出口 IP 的 VPN 则可能被误标 [T03-S024]"
  "确认 2FA 状态: 是否 2FA 设备丢失导致无法登录 (这不是封号而是锁号, 转 WF7)? 是否有未授权的登录尝试触发了安全锁定? 检查绑定邮箱的安全日志 [T03-S003] [T03-S016]"
  "OFAC SDN 自查: 访问 https://sanctionssearch.ofac.treas.gov/ 搜索自己的姓名 — 如果你的名字与 SDN 名单上的条目高度相似, 可能是名字误匹配; 同时搜索 https://www.trade.gov/consolidated-screening-list (CSL) 交叉验证 [T03-S018] [T03-S019]"
  "DMCA 相关检查: 如果提示 \"DMCA\", 检查 https://github.com/github/dmca 仓库中是否有针对你仓库的 takedown notice; 确认你的代码是否包含他人版权材料 (即使是 fork 也可能被投诉); 检查是否有人对你提交了虚假 DMCA (可提 counter-notice) [T03-S007] [T03-S028]"
  "第三方集成审计: 是否授权了可疑的 OAuth App 或 GitHub App? 是否使用了 Copilot/第三方解码工具引入了恶意代码? pilot2254 案例中, 仅有 Copilot Chat tokens 的生成记录却触发了安全封号 [T03-S009] [T03-S025]"
  "形成诊断结论: 将发现写成结构化文档 — 最可能原因 / 支撑证据 / 自己的解释, 这份文档直接成为申诉信的核心素材 (转 WF4) [T03-S001]"
)
declare -a FAILURE_MODES=(

)
declare -a STEP_STATUS=()
declare -a STEP_NOTES=()
declare -a FAIL_CHECK=()

ms_section "${INDUSTRY} · ${WORKFLOW} 走查"
ms_info "逐步走完 ${#SOP_STEPS[@]} 步入门 SOP。每步可标 done / skipped / blocked。"
echo

for i in "${!SOP_STEPS[@]}"; do
  ms_section "Step $((i+1))/${#SOP_STEPS[@]}: ${SOP_STEPS[$i]}"
  ms_prompt "状态? [d=done / s=skipped / b=blocked]"
  read -r status
  STEP_STATUS+=("${status:-d}")
  case "${status:-d}" in
    s) ms_prompt "跳过原因 (一句话):"; read -r note ;;
    b) ms_prompt "blocker (一句话):"; read -r note ;;
    *) note="" ;;
  esac
  STEP_NOTES+=("$note")
done

if [[ ${#FAILURE_MODES[@]} -gt 0 ]]; then
  ms_section "失败模式自检"
  for fm in "${FAILURE_MODES[@]}"; do
    if ms_confirm "你是否避免了 — '$fm' ?"; then
      FAIL_CHECK+=("✓ $fm")
    else
      FAIL_CHECK+=("✗ $fm")
    fi
  done
fi

ms_buffer_append "# ${INDUSTRY} — ${WORKFLOW} 走查报告"
ms_buffer_append ""
ms_buffer_append "_生成时间: $(date '+%Y-%m-%d %H:%M:%S')_"
ms_buffer_append ""
ms_buffer_append "## SOP 步骤"
ms_buffer_append ""
for i in "${!SOP_STEPS[@]}"; do
  status_icon="?"
  case "${STEP_STATUS[$i]}" in
    d|D) status_icon="✓" ;;
    s|S) status_icon="↷" ;;
    b|B) status_icon="✗" ;;
  esac
  if [[ -n "${STEP_NOTES[$i]}" ]]; then
    ms_buffer_append "$((i+1)). [${status_icon}] ${SOP_STEPS[$i]} — ${STEP_NOTES[$i]}"
  else
    ms_buffer_append "$((i+1)). [${status_icon}] ${SOP_STEPS[$i]}"
  fi
done
ms_buffer_append ""
if [[ ${#FAIL_CHECK[@]} -gt 0 ]]; then
  ms_buffer_append "## 失败模式自检"
  ms_buffer_append ""
  for line in "${FAIL_CHECK[@]}"; do
    ms_buffer_append "- $line"
  done
  ms_buffer_append ""
fi

if [[ $JSON -eq 1 ]]; then
  ms_emit_json \
    "workflow" "${WORKFLOW}" \
    "step_count" "${#SOP_STEPS[@]}" \
    "failure_check_count" "${#FAILURE_MODES[@]}"
elif [[ $DRY_RUN -eq 1 ]]; then
  ms_emit_md_stdout
else
  ms_emit_md_file "root-cause-diagnosis-walkthrough"
fi
